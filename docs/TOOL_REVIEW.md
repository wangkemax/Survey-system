# 三件套工具评估 · TOOL REVIEW

> 对照 PLAN「回顾三件套工具，记录哪些好用 / 哪些不好用」。
> 方法：通读三个工具源码 + 结合样板项目实战。结论按"好用 / 不好用 / 发现的问题"组织，末尾给 v1.0 升级优先级。
> 评估日期：2026.05 · 版本：升级后（含行业模板 / 投资测算器 / 订单口径校验 / 数据打通）
> 更新：2026.05.30 已完成"去除硬编码客户名"一轮，相关问题标注为 ✅ 已修复。

---

## 1. site_survey.html（现场物理 + 机会评分）

**好用**

- 7 维评分 + 物理 5 大约束分组（含"业主方改造许可"），覆盖完整，新人照着填不会漏。
- 自动选型提示 + 投资分层建议，把老顾问的判断固化进工具。
- localStorage 自动保存、微信兼容的自定义确认弹窗，现场离线可用。
- 行业模板切换（4 行业）、满分与导出表列随维度动态生成。

**不好用 / 局限**

- 工位推荐用 `name.includes('拣货')` 这类关键词匹配 → 工位名不含特定词就落到"需详细方案讨论"，自定义工位命中率低。
- 切换行业模板后，旧维度的已打分数仍留在 `state.scores`（无害，但不在新模板导出里体现），用户可能困惑。

**已修复**

- ✅ 导出报告标题原硬编码客户名 → 已改为通用「仓库自动化升级 · 现场调研报告」。
- ⏳ 仍待做：增加「客户名」元信息字段，让标题/报告头自动带上当前客户名（见第 5 节 P0）。

---

## 2. deep_survey.html（系统 / 订单 / 流程）

**好用**

- 三 Tab（订单流程 / 系统现状 / 异常访谈）结构清晰，数据拉取清单按优先级排序很实用。
- 滑块百分比 + 严重度评分 + 三角色访谈卡（含金句收集），软性信息也能结构化。
- 订单口径校验——填「件/单 < 行/单」「峰 < 均」等不合理值会实时预警，直接落实经验 2/3。

**不好用 / 局限**

- 部分行业特定文案（数据拉取清单等）偏工程机械配件场景，跨行业时个别项不适用。

**已修复**

- ✅ 原 placeholder / hint / 系统对接段里的具体客户名（多处）已改为中性"客户"。
- ✅ 导出报告标题去硬编码客户名。
- ⏳ 仍待做：加「客户名」字段。

---

## 3. layout_solution.html（方案可视化）

**好用**

- SVG 平面布局 + 四区详解 + 投资矩阵 + 分期 + ASRS/Shuttle 选型对比，客户看着专业。
- 投资测算器（改 SKU/面积自动重算）+ 数据假设与置信度段，从纯静态变可交互。

**不好用 / 局限**

- 本质是「样板案例的方案范例」而非空白模板：分区与设备清单是具体项目的。换项目仍要较多手改 HTML。
- 与 `cases/liugong/03_方案文档.html` 内容高度重叠（仅 ~80 行 diff），工具版与案例版边界模糊。
- 除测算器外无交互，分区/设备/投资都要改源码。

**已修复 / 待决策**

- ✅ 文中硬编码客户名已去除（号线业务逻辑保留，仅去客户前缀）。
- ⏳ 需明确定位：① 改造成「空白可填模板」（字段化，表单驱动生成方案）；或 ② 明确标注它是"样板范例，复制后手改"，并与案例版去重。

---

## 4. 共性问题（三件套通用）

1. ✅ **硬编码客户名** — 已去除（2026.05.30），三件套报告标题与文案现为中性。
2. ⏳ **客户名字段缺失** — 仍建议统一加 `客户名` meta 字段，报告标题用 `${客户名} · xxx报告`，并跨工具共享。
3. **localStorage key 各自独立** — 已能通过统一 JSON 包合并导出打通，但仍无跨工具自动跳转 / 共享客户名。
4. **layout 工具定位** — 静态样板 vs 可填模板需拍板。

---

## 5. v1.0 升级优先级建议

| 优先级 | 事项 | 工具 | 状态 |
|---|---|---|---|
| P0 | 报告标题去硬编码客户名 | 三件套 | ✅ 已完成 |
| P0 | 清理案例特定文案为中性 | deep / layout | ✅ 已完成 |
| P0 | 加「客户名」meta 字段，标题自动带客户名 | 三件套 | ⏳ 待做 |
| P1 | 工位推荐从关键词匹配改为可选标签/下拉 | site | ⏳ 待做 |
| P1 | 跨工具共享客户名 + 元信息（一次填多处用） | 三件套 | ⏳ 待做 |
| P2 | layout 定位拍板：空白可填模板 or 样板范例去重 | layout | ⏳ 待决策 |
| P2 | 行业模板联动物理约束 / 数据拉取清单 | site / deep | ⏳ 待做 |

> 已完成 P0 的"去客户名"；下一步建议补"客户名字段"，再结合团队试用反馈推 P1/P2。
